<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.teachermate.dao.QuestionDao">
    <insert id="create" parameterType="Question" keyProperty="id" useGeneratedKeys="true">
        insert into question (teacherId, pic_content, answer_content, strict,
                              answer_duration, answer_type,
                              answers, content, difficult_id, chapter_id, courseId)
        VALUES (#{teacherId}, #{pic_content}, #{answer_content}, #{strict},
                #{answer_duration}, #{answer_type},
                #{answers},#{content},#{difficult_id},#{chapter_id},#{courseId});
    </insert>
</mapper>